import React from 'react'
import Cards from './Cards'
import SideBar from './SideBar'

type Props = {}

const Home = (props: Props) => {
  return (
    <div>
    <SideBar/>
    </div>
  )
}

export default Home